# 补充全局规则相对路径优先

- 日期：2026-05-10
- 来源：AI 对话摘要
- 类型：记录
- 相关目录：D:\workspace\rethink
- 相关 skill：record-and-reflect-review, delegation-orchestrator
- 标签：AGENTS，全局规则，路径约定

## 任务输入摘要

- 最终结果：在全局 `AGENTS.md` 规则模板中补充“非必要不用绝对路径，默认用相对路径”的约定。
- 现有素材：项目内现有全局规则模板、历史工作记录，以及用户直接给出的新增规则内容。
- 明确约束：只修改本次相关模板与记录；任务很小，不额外拆给 subagent。
- 完成标准：模板文件中出现明确、可复用的相对路径优先规则，且本次修改被记录到项目内。
- 产出后动作：如需让当前机器立即生效，再同步更新本机全局 `AGENTS.md`。

## 背景

用户希望继续完善全局协作规则，补上一条路径表达规范，避免在不必要的场景里默认输出绝对路径。

## 关键过程

- 读取了项目中的 `record-and-reflect-review` 与 `delegation-orchestrator`，确认本次需要记录到项目内，并判断任务过小、不值得委派。
- 检查仓库根目录后确认不存在实际的 `AGENTS.md` 文件，因此将本次修改目标收敛到全局规则模板 `skills/record-and-reflect-review/references/global-agents-rules.md`。
- 先查看 Git 状态，确认工作区干净，再在 `## 需求确认与行动边界` 小节补入路径约定，保持与现有规则语气一致。

## 结果

- 已修改 `skills/record-and-reflect-review/references/global-agents-rules.md`。
- 新增规则内容：提及文件、目录、命令参数或配置位置时，非必要不用绝对路径，默认使用相对路径；仅在消除歧义、跨工作区定位或工具明确要求时才使用绝对路径。
- 已新增本次工作记录文件，避免混入其他主题记录。
- 验证：已人工复核模板位置与措辞，确认改动范围仅限本次需求。

## 可复用经验

- 这类协作约定应优先落在模板源文件，而不是临时改对话里的引用文本，否则后续安装或同步时容易丢失。
- 路径规则适合写成“默认值 + 例外条件”的形式，比绝对化表述更稳妥。

## Skill 观察

- 是否出现新 skill 候选：否。
- 是否应该优化已有 skill：暂不需要，现有记录与委派 skill 已足够支撑这类小型规则维护。
- 触发条件或典型用户说法：全局规则中需要补充；非必要不用绝对路径；默认用相对路径。

## 后续事项

- [ ] 如需让当前机器立即生效，可再同步更新本机全局 `C:\Users\Administrator\.codex\AGENTS.md`。
