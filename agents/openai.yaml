interface:
  display_name: "记录以及反思回顾"
  short_description: "记录 AI 工作摘要，并沉淀可复用 skill"
  default_prompt: "Use $record-and-reflect-review to record this thread and review whether the work should become a new or improved skill."

policy:
  allow_implicit_invocation: true
